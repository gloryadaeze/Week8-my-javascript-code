function minsToSec(m) {
    let m = minutes;
    const seconds = minutes * 60;
    return seconds;
  }
  
  console.log(`${minsToSec(5)} minutes is equal to ${seconds} seconds.`);
  // Example usage:
  //const minutes = 5;
  //const result = minutesToSeconds(minutes);
  //console.log(`${minutes} minutes is equal to ${result} seconds.